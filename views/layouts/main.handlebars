<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.1/dist/css/bootstrap.min.css"
    integrity="undefined" crossorigin="anonymous">
  <link rel="stylesheet"
    href="https://cdn.jsdelivr.net/npm/@fortawesome/fontawesome-free@5.15.3/css/fontawesome.min.css"
    integrity="undefined" crossorigin="anonymous">
  <link rel="stylesheet" href="/css/style.css" type="text/css">
    <title>Givehub</title>
</head>
<body>
    <header class="site-header py-1">
        <nav class="container d-flex flex-column flex-md-row justify-content-around">
          {{#if logged_in}}
          <a class="py-2 d-none d-md-inline-block" href="#">Profile</a>
          <button class="no-button" id="logout">Logout</button>
          {{else}}
          <a class="py-2 d-none d-md-inline-block" href="#">Login</a>
          {{/if}}
        </nav>
      </header>
    <main class="container container-fluid mt-5">
      <div class="position-relative overflow-hidden p-3 p-md-5 m-md-3 text-center" id="hero">
        <div class="col-md-5 p-lg-5 mx-auto my-5"></div>
      </div>
      <div class="position-relative overflow-hidden p-3 p-md-5 m-md-3 text-center">
        <div class="col-md-5 p-lg-1 mx-auto my-2"></div>
        <h1 class="display-4 fw-normal">CHARITIES</h1>
        <h2 class="lead fw-normal">CATEGORIES</h2>
        {{{ body }}}
      </div>
    </main>
</body>
</html>